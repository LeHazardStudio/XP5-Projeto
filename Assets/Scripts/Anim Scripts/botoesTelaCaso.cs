using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class botoesTelaCaso : MonoBehaviour
{
    public GameObject bTelaCaso;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void AtivarBotoesTelaCaso()
    {
        bTelaCaso.SetActive(true);
    }
}
